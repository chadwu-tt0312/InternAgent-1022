# AutoMaterial 案例實作完成總結（2025-02 更新）

## 🎯 本次重製成果
- **CGCNN baseline 重構**：`experiment.py` 現在僅保留完整的 CGCNN 訓練流程，並新增 `--dataset_source` 參數以切換 MatBench 或合成晶體資料；`--mode full` 僅用於相容性，實際控制以新參數為主。
- **合成資料支援**：離線環境可透過 pymatgen 動態生成晶格、原子與形成能標註，仍沿用相同圖構建邏輯，確保模型/評估一致。
- **文件同步**：`中文說明.md` 重新撰寫環境設定、執行指令與 FAQ，加入 `uv run`、`--dataset_source`、`--synthetic_size` 等操作說明。

## 📁 主要檔案
| 檔案 | 狀態與重點 |
| --- | --- |
| `prompt.json` | 維持原始任務描述與研究背景 |
| `experiment.py` | 全新 CGCNN pipeline，支援 MatBench/合成/自定 JSON 資料、ReduceLROnPlateau、best model snapshot |
| `中文說明.md` | 新增 uv 安裝建議、MatBench 與 synthetic 指令示例、參數列表更新並補充合成資料設計、離線操作、FAQ 更新 |
| `run_0/experiment.py` | 保留初始版本供參考 |

## ⚙️ 執行方式
```bash
# 建議先同步依賴
uv sync

# MatBench（需可安裝 matbench==0.6）
uv run python tasks/AutoMaterial/experiment.py \
    --dataset_source matbench \
    --epochs 100 \
    --save_model

# 合成資料（離線/無法安裝 matbench 時）
uv run python tasks/AutoMaterial/experiment.py \
    --dataset_source synthetic \
    --use_subset --subset_size 2048 \
    --epochs 30

# 結果
cat tasks/AutoMaterial/output/final_info.json
```

## ✅ 驗證與輸出
- 指令完成後皆會生成 `tasks/AutoMaterial/output/final_info.json`，內容符合 InternAgent 指標格式。
- 若啟用 `--save_model`，會額外輸出 `best_model.pt`。
- 彙報中已示範 `--dataset_source synthetic` 的完整訓練流程並確認 MAE 會持續下降。

## 🔄 後續建議
1. **安裝 matbench**：若能取得 `scikit-learn==1.0.1` 相容 wheel，可改以真實 MatBench 進行訓練。
2. **Ideas 實作**：依 `prompt.json` 的建議（長程注意力、多尺度特徵）在現有 CGCNN baseline 上實驗。
3. **數據擴充**：可透過 `--data_path` 餵入自行蒐集的 CIF 或結構參數，以檢驗模型遷移能力。
